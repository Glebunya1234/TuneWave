
const HiddenBar = (text:string, isClose: boolean): { value: string; flag: boolean } => {

    const timer2 = setTimeout(() => {
        return timer2
    }, 500);


    return {
        value: "style.MediaLibrary__remove",
        flag: !isClose
    };
};