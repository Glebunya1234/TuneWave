// файл: components/SSS.jsx (или .tsx)

"use server"; // Используйте эту директиву только если она необходима для серверного рендеринга

export async function SSS() {
  // Вы можете выполнять асинхронные операции здесь, если это нужно
  return <p>Helilil</p>;
}
